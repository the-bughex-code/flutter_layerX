name: layerx_generator
description: A Flutter package that auto-generates a structured LayerX directory with GetX integration, MVVM pattern, and essential services for scalable projects.
version: 2.1.0
homepage: https://github.com/the-bughex-code/flutter_layerX
repository: https://github.com/the-bughex-code/flutter_layerX
issue_tracker: https://github.com/the-bughex-code/flutter_layerX/issues

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  args: ^2.7.0
  path: ^1.9.1
  yaml: ^3.1.2

dev_dependencies:
  test: ^1.28.0
  lints: ^6.0.0

executables:
  layerx: layerx
  layerx_generator: layerx_generator

topics:
  - flutter
  - getx
  - mvvm
  - codegen
  - cli
